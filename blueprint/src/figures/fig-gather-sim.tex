% The three tier simulations of one gather-based round
% (PLTS.ABA.GBCA.ByAFW.lowPairRefines, idealRefines, pairRefines).
% Concrete rows above each tier, the tier below, dashed verticals the relation.
\begin{tikzpicture}[
    >={Stealth[round]}, semithick, on grid,
    conc/.style={draw, rounded corners, minimum height=0.8cm, align=center,
      font=\footnotesize, inner sep=3pt},
    lvl/.style={draw, rounded corners, minimum width=3.1cm, minimum height=0.8cm,
      align=center, font=\footnotesize, inner sep=3pt},
    note/.style={font=\scriptsize, align=left}]
  \node[lvl] (low)   at (0,1.95)  {$\mathrm{roundOverBracha}$};
  \node[lvl] (ideal) at (0,0.35)  {$\mathrm{roundOverBroadcastSpecification}$};
  \node[lvl] (pair)  at (0,-1.25) {$\mathrm{roundOverGatherSpecifications}$};
  \node[lvl] (spec)  at (0,-2.85) {$\mathrm{GBCA.specInst}$};
  \draw[->, thick] (low)   -- (ideal);
  \draw[->, thick] (ideal) -- (pair);
  \draw[->, thick] (pair)  -- (spec);
  \node[conc] (r1) at (5.6,1.15)
    {every $\mathrm{ECHO}$, $\mathrm{VOTE}$, delivery\\ and return of a Bracha instance};
  \node[conc] (r2) at (5.6,-0.45)
    {every $\mathrm{ECHO}$, $\mathrm{VOTE}$ and $\mathrm{BIND}$\\ of a gather instance};
  \node[conc] (r3) at (5.6,-2.05)
    {a return whose exclusion is missing;\\ a return whose exclusion has happened};
  \draw[dashed, ->] (r1) -- (1.65,1.15) node[note, pos=0.72, above]{$\mathrm{LowPairRel}$};
  \draw[dashed, ->] (r2) -- (1.65,-0.45) node[note, pos=0.72, above]{$\mathrm{IdealRel}$};
  \draw[dashed, ->] (r3) -- (1.65,-2.05) node[note, pos=0.72, above]{$\mathrm{PairRel}$};
  \node[note, anchor=west] at (8.5,1.15)  {a stutter or a return\\ of the BRB specification};
  \node[note, anchor=west] at (8.5,-0.45) {a stutter; a broadcast\\ return writes a store};
  \node[note, anchor=west] at (8.5,-2.05) {a run, or a single\\ graded return};
  \node[font=\footnotesize, align=center] at (5.0,-3.85)
    {each stage idealises away one layer and holds the layers above it equal;\\
     the two upper stages are congruence applications, the layer's network held equal,\\
     and $\mathrm{PairRel}$ pins $excluded$ to the complement of the bit it holds};
\end{tikzpicture}
