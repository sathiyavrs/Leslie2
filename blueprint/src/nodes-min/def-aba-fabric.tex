\begin{definition}[The two-box instance vocabulary]
  \label{def:aba-fabric}
  \lean{PLTS.ABA.Fabric}\lean{PLTS.ABA.Box}\lean{PLTS.ABA.SubState}\lean{PLTS.ABA.SubState.corrupt}\lean{PLTS.ABA.SubState.mcast}\lean{PLTS.ABA.SubState.recvMsg}\lean{PLTS.ABA.SubState.recvCount}\leanok
  \uses{def:aba-params}
  The state shape the broadcast and gather encodings share: a message fabric holding the per-sender pools and the corrupted set (D5, D1) beside $n$ process boxes, each holding one local record and its per-sender inboxes.
  Multicast, delivery, corruption and the receipt counts are operations on the pair, and the quorum-intersection lemmas are stated once over it: \leandecl{PLTS.ABA.SubState}.
\end{definition}
