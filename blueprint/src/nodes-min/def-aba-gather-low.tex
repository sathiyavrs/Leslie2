\begin{definition}[The gather implementation over Bracha instances]
  \label{def:aba-gather-low}
  \lean{PLTS.ABA.Gather.LowState}\lean{PLTS.ABA.Gather.LowStep}\lean{PLTS.ABA.Gather.lowInst}\leanok
  \uses{def:aba-gather-ideal, def:aba-brb-impl}
  The table of Definition~\ref{def:aba-gather-ideal} with each broadcast-specification coordinate replaced by a Bracha instance of Definition~\ref{def:aba-brb-impl}: the gather network state beside $2n$ implementation states, approval and bind commitment read as $n{-}f$ $\mathrm{VOTE}$ receipt-quorum predicates on them (D28): \leandecl{PLTS.ABA.Gather.lowInst}.
  The core is auxiliary state here too, written by the first return and carried on every return label (D29).
\end{definition}
