\begin{definition}[The gather-based GBCA implementation]
  \label{def:aba-gbca-gather-low}
  \lean{PLTS.ABA.GBCA.IdealState}\lean{PLTS.ABA.GBCA.IdealStep}\lean{PLTS.ABA.GBCA.idealInst}\lean{PLTS.ABA.GBCA.LowPairState}\lean{PLTS.ABA.GBCA.LowPairStep}\lean{PLTS.ABA.GBCA.lowPairInst}\leanok
  \uses{def:aba-gbca-pair, def:aba-gather-ideal, def:aba-gather-low}
  The two-gather round at its two concrete readings, one rule table each over the shared round alphabet: over gather-over-broadcast components, and fully concrete over gather-over-Bracha components --- the two gather instances and, beneath them, the $4n$ Bracha instances carrying the inputs and the $\mathrm{BIND}$ payloads.
  The concrete reading \leandecl{PLTS.ABA.GBCA.lowPairInst} is the gather-based GBCA implementation, the protocol as it runs.
\end{definition}
