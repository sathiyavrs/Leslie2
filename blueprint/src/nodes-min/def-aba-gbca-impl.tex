\begin{definition}[The GBCA implementation instance]
  \label{def:aba-gbca-impl}
  \lean{PLTS.ABA.GBCA.implInst}\leanok
  \uses{def:aba-labels, def:aba-params, def:aba-gbca-vocab}
  Graded agreement read as one per-round PLTS over the pair of local states that hold a round's state --- the $n$ stage records beside the round's message state --- transcribing ABDY22's Algorithm~6 over the five message levels (D18): \leandecl{PLTS.ABA.GBCA.implInst}.
\end{definition}
