\begin{definition}[The GBCA implementation instance]
  \label{def:aba-gbca-implementation}
  \lean{PLTS.ABA.GBCA.ByABDY.boundOf}\lean{PLTS.ABA.GBCA.ByABDY.implementation}\leanok
  \uses{def:aba-labels, def:aba-parameters, def:aba-round-messages}
  Graded agreement read as one per-round PLTS over the pair of local states that hold a round's state --- the $n$ round records beside the round's network state --- transcribing ABDY22's Algorithm~6 over the five message levels (D18): \leandecl{PLTS.ABA.GBCA.ByABDY.implementation}.
  The network state holds one field more, the round's bound bit: the three return rows announce \leandecl{PLTS.ABA.GBCA.ByABDY.boundOf} of the round's sent sets, the corrupted set and the outcome where the round has no bit on record, and the bit on record otherwise, and write it back (D29).
\end{definition}
