\begin{definition}[Strong probabilistic simulation]
  \label{def:strongsim}
  \lean{PLTS.StrongProbabilisticSimulation}\leanok
  \uses{def:system, def:coupling}
  A strong probabilistic simulation is a relation on states, holding at the two initial states, in which every concrete step is answered by a single abstract step on the same label whose successor distributions are coupled: \leandecl{PLTS.StrongProbabilisticSimulation}.
\end{definition}
