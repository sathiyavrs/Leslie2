\begin{theorem}[The gather-based composition]
  \label{thm:aba-protocolg-composedg}
  \lean{PLTS.ABA.NetG.toPair}\lean{PLTS.ABA.NetG.ProtocolRelG}\lean{PLTS.ABA.protocolSimG}\lean{PLTS.ABA.protocolG_composedG}\leanok
  \uses{def:aba-protocolg, def:aba-gather-side, thm:forward-sound, def:achievable}
  The gather-based protocol is forward-simulated by its composed reading, along the Dirac lift of a relation that computes the composed state from the flat one, and hence $\operatorname{ATD}(\mathrm{protocolG}(P)) \subseteq \operatorname{ATD}(\mathrm{composedG}(P))$: \leandecl{PLTS.ABA.protocolSimG}, \leandecl{PLTS.ABA.protocolG_composedG}.
\end{theorem}

\begin{proof}\leanok
  \uses{def:aba-protocolg, def:aba-gather-side, def:forwardsim}
  See the Lean proof of \leandecl{PLTS.ABA.protocolSimG}.
\end{proof}
