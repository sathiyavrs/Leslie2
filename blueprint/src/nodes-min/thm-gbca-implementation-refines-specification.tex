\begin{theorem}[The GBCA implementation refines its specification]
  \label{thm:gbca-implementation-refines-specification}
  \lean{PLTS.ABA.GBCA.ByABDY.refinesSpecification}\leanok
  \uses{def:aba-gbca-implementation, def:aba-gbca-spec}
  The GBCA implementation instance is forward-simulated by the GBCA specification instance, along a relation certifying the exclusion set and the grade by receipt patterns and firing an exclusion on demand: \leandecl{PLTS.ABA.GBCA.ByABDY.refinesSpecification}.
\end{theorem}

\begin{proof}\leanok
  \uses{def:aba-gbca-implementation, def:aba-gbca-spec, def:forwardsim}
  See the Lean proof of \leandecl{PLTS.ABA.GBCA.ByABDY.refinesSpecification}.
\end{proof}
