\begin{definition}[Probabilistic execution and its path measure]
  \label{def:probabilistic-execution}
  \lean{PLTS.ProbabilisticExecution}\leanok
  \uses{def:system, def:exec}
  A \emph{probabilistic execution} of $sys$ is an initial distribution $initState : \operatorname{PMF} State$ together with a \emph{scheduler} that, at each finite history, returns a sub-distribution over the next $(l, \mu)$ move (or halts).
  For a finite execution $e$ it induces a mass $probOf(e) \in [0, \infty]$ (\leandeclnamed{probOf}{PLTS.ProbabilisticExecution.probOf}), the probability that the scheduler, started from $initState$, follows exactly the path $e$: the initial mass on $e.init$ times the product of the one-step kernels along $trans$.
\end{definition}
