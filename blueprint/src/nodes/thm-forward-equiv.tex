\begin{theorem}[Forward simulation $=$ strong simulation into $\mathcal{D}(\cdot^w)$]
  \label{thm:forward-equiv}
  \lean{PLTS.probabilisticForwardSimulation_iff_strong_dist_weakClosure}\leanok
  \uses{def:forwardsim, def:strongsim, def:dist, def:weakclosure, lem:hyperstep-weakstep}
  For any relation $R : State_C \to \operatorname{PMF} State_A \to \mathrm{Prop}$,
  \[
    \text{$R$ is a forward simulation of $sys_C$ by $sys_A$}
    \iff
    \text{$R$ is a strong simulation of $sys_C$ by $\mathcal{D}(sys_A^w)$.}
  \]
\end{theorem}

\begin{proof}\leanok
  \uses{lem:hyperstep-weakstep}
  Both notions carry the \emph{same} relation $R$, the state type of $\mathcal{D}(sys_A^w)$ being $\operatorname{PMF} State_A$, which is the type of the forward simulation's abstract argument.
  On initial states, the forward simulation's distribution supported on $sys_A.init$ is forced to be $\delta_{sys_A.init}$, the initial state of $\mathcal{D}(sys_A^w)$, and the internal-versus-external case split coincides because $\tau$ is canonical.
  The step clauses then match verbatim under the correspondence (Lemma~\ref{lem:hyperstep-weakstep}), the same coupling $\omega$ and the same $\operatorname{PMFRel} R\ \mu_C\ \omega$ witnessing both.
\end{proof}
