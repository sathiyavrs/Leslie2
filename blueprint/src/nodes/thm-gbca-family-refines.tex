\begin{theorem}[Family congruence and the graded-agreement family refinement]
  \label{thm:gbca-family-refines}
  \lean{PLTS.ForwardSimulation.family}\lean{PLTS.ABA.gbcaSpecificationFamily}\lean{PLTS.ABA.substitutionRelationFamily}\lean{PLTS.ABA.familySubstitution}\lean{PLTS.ABA.familySubstitutionSimulation}\leanok
  \uses{thm:aba-subsim, def:aba-gbcasub, def:idle-family, lem:hyperstep-weakstep}
  Per-instance forward simulations lift to the $\mathbb{N}$-indexed families under the pointwise relation, broadcast labels being matched by the abstract family's own broadcast under a corruption-compatibility lemma.
  Applied to Theorem~\ref{thm:aba-subsim}, the lift says that the family $\mathrm{gbcaInstanceFamily}(P)$ of round instances is forward-simulated, round by round, by the family $\mathrm{gbcaSpecificationFamily}(P)$ of round specifications read over the same interface.
  Both families are LTS, so the LTS correspondence turns this into a probabilistic forward simulation, applied once, at family level.
\end{theorem}

\begin{proof}\leanok
  \uses{thm:aba-subsim, def:idle-family, lem:hyperstep-weakstep}
  The two families are built by the same construction from the same ownership map $\mathrm{roundOwnsLabel}$, so an owned or silent label moves the one instance that owns it and is matched instance-wise by Theorem~\ref{thm:aba-subsim}, while a label owned by no round leaves both families in place.
  A corruption broadcast is matched by the abstract family's own broadcast, using the compatibility lemma of Theorem~\ref{thm:aba-subsim} at every instance at once.
  Each round instance is an LTS and so is each lifted specification instance, so a family of them is an LTS by Definition~\ref{def:idle-family}, and the correspondence (Lemma~\ref{lem:hyperstep-weakstep}) applies once, at family level, yielding a probabilistic forward simulation along the Dirac lift of the pointwise relation.
\end{proof}
