% This file makes the printable version of the full edition of the blueprint.
% It shares macros and figures with the default edition (print.tex); its
% connective prose (content-full.tex) and its node bodies (nodes/, against the
% default edition's nodes-min/) are its own, and
% scripts/check-node-sync.py keeps the frontmatter of each node pair in sync.

\documentclass[a4paper]{report}

\usepackage{geometry}

\usepackage{expl3}

\usepackage{amssymb, amsthm, mathtools}
\usepackage[unicode,colorlinks=true,linkcolor=blue,urlcolor=magenta, citecolor=blue]{hyperref}

\usepackage[warnings-off={mathtools-colon,mathtools-overbracket}]{unicode-math}

\usepackage{algorithm}
\usepackage{algpseudocode}

\usepackage{tikz}
\usetikzlibrary{automata,positioning,arrows.meta,quotes,shapes.geometric}

% Prefer a float at the top of a text page over a page of floats alone.
\renewcommand{\floatpagefraction}{0.85}
\renewcommand{\topfraction}{0.85}
\renewcommand{\textfraction}{0.1}

\input{macros/common}
\input{macros/print}

\title{leslie2 (full edition)}

\begin{document}
\maketitle
\tableofcontents
\input{content-full}
\end{document}
